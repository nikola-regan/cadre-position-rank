# Core data + numerical
pandas>=2.0
numpy>=1.24
scipy>=1.10
openpyxl>=3.1     # for reading CPED .xlsx files

# Statistics + econometrics
statsmodels>=0.14
linearmodels>=5.3 # PanelOLS for within-person FE

# Network estimation
scikit-learn>=1.3 # k-means clustering
numba>=0.58       # JIT compilation for MVR-MCMC

# Visualization
matplotlib>=3.7

# Document handling (only needed if rebuilding the pre-registration PDF)
markdown>=3.5
weasyprint>=60
